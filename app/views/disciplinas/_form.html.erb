<%= form_for(@disciplina) do |f| %>
  <% if @disciplina.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@disciplina.errors.count, "error") %> prohibited this disciplina from being saved:</h2>

      <ul>
      <% @disciplina.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :prof_id_id %><br>
    <%= f.text_field :prof_id_id %>
  </div>
  <div class="field">
    <%= f.label :assunto_id_id %><br>
    <%= f.text_field :assunto_id_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
